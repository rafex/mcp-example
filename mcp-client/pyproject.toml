[project]
name = "mcp-client"
version = "0.1.0"
description = "Cliente MCP de prueba para los servidores de este repositorio"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.27.0",
]
